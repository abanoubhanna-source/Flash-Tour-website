import { NextResponse } from "next/server";
import { getPublishedCollection, publicData } from "@/lib/cms/public-content";
import fallback from "@/data/services.json";
export async function GET() { const entries = await getPublishedCollection("service"); return NextResponse.json(entries.length ? entries.map((entry) => { const data = publicData(entry); return { ...data, id: entry.id, title: entry.title, desc: typeof data.description === "string" ? data.description : typeof data.shortDescription === "string" ? data.shortDescription : typeof data.fullDescription === "string" ? data.fullDescription : "", img: typeof data.image === "string" && data.image ? data.image : typeof (data.image as { url?: unknown })?.url === "string" ? (data.image as { url: string }).url : "/images/services-hero.jpg" }; }) : fallback); }
