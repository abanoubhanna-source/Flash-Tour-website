import type{Metadata}from"next";import{notFound}from"next/navigation";import{requireCmsUser}from"@/lib/auth/session";import{getCmsDestination}from"@/lib/cms/destinations/queries";import{DestinationEditor}from"@/components/cms/destinations/destination-editor";
export const metadata:Metadata={title:"Edit destination"};export default async function Page({params}:{params:Promise<{destinationId:string}>}){const[{destinationId},user]=await Promise.all([params,requireCmsUser()]);const destination=await getCmsDestination(destinationId);if(!destination)notFound();return <DestinationEditor destination={destination} canEdit={user.permissions.includes("content.edit")&&user.permissions.includes("seo.edit")} canPublish={user.permissions.includes("content.publish")&&user.permissions.includes("seo.publish")} canUpload={user.permissions.includes("media.create")}/>}
